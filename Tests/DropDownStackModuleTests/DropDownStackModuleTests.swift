import XCTest
@testable import DropDownStackModule

final class DropDownStackModuleTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(DropDownStackModule.text, "Hello, World!")
    }
}
